---
import Layout from '../layouts/Layout.astro';
import data from '../data/business-data.json';

[cite_start]// Ensure the path has a trailing slash to avoid 404 race conditions [cite: 1]
const successPath = "/success/"; 
---

<Layout title="Contact Us">
  <main class="py-24 px-6 max-w-7xl mx-auto">
    <div class="grid lg:grid-cols-2 gap-16">
      <div>
        <h1 class="text-4xl font-bold text-slate-900 leading-tight">
          Let's Build Something <br /> 
          <span class="text-brand-primary font-extrabold">Extraordinary.</span>
        </h1>
        <p class="mt-6 text-lg text-slate-600">
          [cite_start]Ready to launch your next {data.siteType} project? [cite: 2] 
          [cite_start]Fill out the form, and our team will get back to you within 24 hours. [cite: 3]
        </p>

        <div class="mt-12 space-y-6">
          <div class="flex items-center gap-4">
            <div class="bg-slate-100 p-3 rounded-lg text-brand-primary">📧</div>
            <div>
              <p class="text-sm font-semibold text-slate-900">Email</p>
              <p class="text-slate-600">{data.contact.email}</p>
            </div>
          </div>
          <div class="flex items-center gap-4">
            <div class="bg-slate-100 p-3 rounded-lg text-brand-primary">📞</div>
            <div>
              <p class="text-sm font-semibold text-slate-900">Phone</p>
              <p class="text-slate-600">{data.contact.phone}</p>
            </div>
          </div>
        </div>
      </div>

      <div class="bg-white p-8 border border-slate-200 rounded-2xl shadow-sm">
        <form 
          name="contact" 
          method="POST" 
          data-netlify="true" 
          action={successPath} 
          data-netlify-honeypot="bot-field"
          class="space-y-6"
        >
          <input type="hidden" name="form-name" value="contact" />
          
          <p class="hidden">
            <label>Don't fill this out if you're human: <input name="bot-field" /></label>
          </p>

          <div>
            <label for="name" class="block text-sm font-medium text-slate-700">Full Name</label>
            [cite_start]<input type="text" name="name" id="name" required class="mt-1 block w-full rounded-md border-slate-300 shadow-sm focus:border-brand-primary focus:ring-brand-primary text-slate-900 p-3 border" /> [cite: 8]
          </div>
          
          <div>
            <label for="email" class="block text-sm font-medium text-slate-700">Email Address</label>
            [cite_start]<input type="email" name="email" id="email" required class="mt-1 block w-full rounded-md border-slate-300 shadow-sm focus:border-brand-primary focus:ring-brand-primary text-slate-900 p-3 border" /> [cite: 9]
          </div>
          
          <div>
            <label for="message" class="block text-sm font-medium text-slate-700">Message</label>
            [cite_start]<textarea id="message" name="message" rows="4" required class="mt-1 block w-full rounded-md border-slate-300 shadow-sm focus:border-brand-primary focus:ring-brand-primary text-slate-900 p-3 border"></textarea> [cite: 9]
          </div>
          
          <button type="submit" class="w-full bg-brand-primary text-white font-semibold py-3 px-6 rounded-lg hover:bg-brand-hover transition-all shadow-md active:scale-95">
            [cite_start]Send Message [cite: 10]
          </button>
        </form>
      </div>
    </div>
  </main>
</Layout>